name: tiny
d_model: 128
n_layers: 4
n_heads: 4
d_ff: 512
seq_len: 1024
dropout: 0.0
batch_tokens: 65536      # tokens per optimizer step (across micro-batches)
micro_batch: 32           # sequences per fwd/bwd
warmup_steps: 200
weight_decay: 0.1
grad_clip: 1.0
